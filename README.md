# Привет! Это моя витрина карточек на Micro Frontend архитектуре 🚀

Когда-то я решил поиграться с микрофронтендами и создал это небольшое приложение. Оно показывает карточки с цитатами или задачами, с пагинацией и переключением тем. Ничего сверхъестественного, но зато демонстрирует, как можно организовать модульную архитектуру.

### Ключевые фишки:

- **Микрофронтенды**: Каждый модуль можно разрабатывать и деплоить независимо
- **Web Components**: Карточки сделаны на нативных веб-компонентах - работают где угодно
- **TypeScript везде**: Полная типизация во всех модулях (даже в shared API!)
- **Module Federation**: Webpack магия, которая позволяет модулям общаться друг с другом

## Из чего состоит приложение

Приложение разбито на несколько независимых модулей:

### 🎯 **host-app** (порт 8080)

Главное React-приложение. Здесь живет основная логика: выбор данных, темы, пагинация. Оно собирает все остальные модули в единое целое.

### 🎴 **card-list** (порт 8083)

Модуль с логикой отображения карточек. Получает данные из API, управляет пагинацией, передает данные в компоненты отображения.

### 🔍 **data-selector** (порт 8081)

Простой селектор: "Цитаты" или "Задачи". Выбираешь источник данных - и карточки меняются.

### 🎨 **theme-selector** (порт 8082)

Переключатель тем: светлая/темная. Использует CSS-переменные для мгновенного переключения.

### 🌐 **web-components** (порт 8085)

Здесь живут сами карточки. Сделаны на Web Components - нативных кастомных элементах. Работают без React/Vue/Angular.

### 🔗 **shared** (порт 8084)

Общие утилиты: API-клиент для dummyjson.com. Все остальные модули дергают его за данными.

## Как запустить это чудо?

Легко! Вот пошагово:

### 1. Установи зависимости

```bash
# Установи во всех модулях
cd host-app && npm install
cd card-list && npm install
cd data-selector && npm install
cd theme-selector && npm install
cd web-components && npm install
cd shared && npm install
```

### 2. Запусти все сервисы

Самый простой способ - запустить в отдельных терминалах:

```bash
# Терминал 1
cd shared && npm start

# Терминал 2
cd web-components && npm start

# Терминал 3
cd card-list && npm start

# Терминал 4
cd data-selector && npm start

# Терминал 5
cd theme-selector && npm start

# Терминал 6
cd host-app && npm start
```

### 3. Открой в браузере

Иди на http://localhost:8080 - и готово! 🎉

## Что умеет приложение?

- **Два типа данных**: Цитаты от великих людей или список задач (данные из dummyjson.com API)
- **Пагинация**: По 5 элементов на страницу, с навигацией вперед-назад
- **Темы**: Светлая и темная (переключаются мгновенно через CSS-переменные)
- **Адаптивность**: Работает на десктопе и мобилке
- **Обработка ошибок**: Показывает сообщения, если что-то пошло не так
- **Загрузка**: Индикатор пока данные подгружаются

## Технический стек

- **React 19** с хуками для всего
- **TypeScript** везде (даже в shared модуле!)
- **Webpack 5** с Module Federation
- **Web Components** для карточек
- **Axios** для API запросов
- **CSS Custom Properties** для тем

## P.S.

Если что-то не работает - проверь, что все порты 8080-8085 свободны. Иногда на macOS они заняты системными сервисами. И да, код не идеальный, но работает! 😄
